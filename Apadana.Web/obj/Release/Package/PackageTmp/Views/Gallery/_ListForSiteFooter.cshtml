@for (var i = 1; i <= 8; i++)
{
    <li>
        <a href="@Url.Action("Index", null, new { area = string.Empty, controller = "Gallery" }, Request.Url.Scheme)">
            <img alt="" src="~/upload/flickr/flickr@(i).jpg">
        </a>
    </li>
}